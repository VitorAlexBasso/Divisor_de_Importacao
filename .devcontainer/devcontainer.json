{
  "name": "Python 3 - Streamlit",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-python.vscode-pylance"]
    },
    "codespaces": {
      "openFiles": ["README.md", "App.py"]
    }
  },
  "updateContentCommand": "[ -f packages.txt ] && sudo apt-get update && sudo xargs -a packages.txt apt-get install -y; [ -f requirements.txt ] && python -m pip install --no-cache-dir -r requirements.txt; echo '✅ env ok'",
  "postAttachCommand": "python -m streamlit run App.py --server.enableCORS false --server.enableXsrfProtection false",
  "portsAttributes": {
    "8501": { "label": "Application", "onAutoForward": "openPreview" }
  },
  "forwardPorts": [8501]
}
